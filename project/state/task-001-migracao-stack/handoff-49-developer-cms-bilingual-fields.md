STATUS: COMPLETED
SUMMARY: Corrigido um bug crítico em `public/admin/config.yml`: 79 pares de campos bilingues estavam definidos com `name: "campo.pt"` / `name: "campo.en"` (nomes de chave literais com ponto), padrão que o Decap CMS não interpreta como caminho aninhado — em vez disso cria uma chave nova e plana `"campo.pt"` ao lado do objeto `campo: {pt, en}` real, sem nunca escrever nele. Todos os 79 pares foram convertidos para o padrão correto já usado no resto do ficheiro (`widget: object` com `fields: [{name: pt}, {name: en}]`), preservando indentação, `widget: text` vs `string`, e `required: true/false` de cada campo original. Também corrigidos os dados já corrompidos em produção em `content/site/team.json` (recuperando os valores reais que o utilizador escreveu via `/admin` e removendo as chaves mortas `"cargo.pt"`/`"cargo.en"`).

Reprodução do bug antes de corrigir: li `content/site/team.json` tal como estava commitado (PR #11 já mergeado) e confirmei visualmente a chave morta `"cargo.pt": "CEO"` / `"cargo.en": "CEO"` ao lado de `"cargo": {"pt": "Cargo Exemplo", "en": "Example Role"}` — ou seja, a escrita do utilizador nunca chegou ao objeto real que `content/schemas/index.ts` valida e que o site lê. Isto confirmou a causa raiz descrita no pedido antes de qualquer alteração.

ARTIFACTS:
- `public/admin/config.yml` — 79 pares de campos convertidos de `name: "campo.pt"/"campo.en"` para blocos `widget: object` aninhados. Nenhuma outra linha do ficheiro foi tocada (backend, publish_mode, media_folder, comentários existentes, etc. mantidos exatamente como estavam).
- `content/site/team.json` — `cargo` real dos 3 membros corrigido para os valores que o utilizador escreveu (Cipriano: `{pt: "CEO", en: "CEO"}`; Nome Exemplo 2: `{pt: "Maquinista", en: "Worker"}`; Nome Exemplo 3: `{pt: "Vendedor", en: "Saler"}` — mantido literal "Saler", não corrigi a ortografia). Chaves mortas `"cargo.pt"`/`"cargo.en"` removidas dos três objetos. Estrutura `{"items": [...]}` do ficheiro preservada, sem alterações.
- `C:\Users\VIRGIL~1.JOS\...\scratchpad\fix_config.py` — script Python usado para a conversão mecânica dos 79 pares (fora do repo, só ferramenta de trabalho, não faz parte dos artefactos a commitar).

VALIDATIONS:
1. `grep -n '"[a-zA-Z]*\.\(pt\|en\)"' public/admin/config.yml` (grep do enunciado, passo 1) — 0 resultados no final. Nota: este grep tem um falso-negativo — não apanha nomes com dígitos (ex.: `titleLine1.pt`) porque `[a-zA-Z]*` não inclui `1`. Corri também `grep -n 'name: "[a-zA-Z0-9]*\.[a-zA-Z0-9]*"'` (mais abrangente, cobre qualquer nome pontuado incluindo dígitos e nomes de dois níveis como `buttons.whatsapp.pt`) — também 0 resultados no final. Recomendo ao Tester usar esta segunda variante como grep de regressão, não só a do enunciado.
2. `node -e "const yaml=require('js-yaml'); yaml.load(...); console.log('OK')"` — OK, YAML válido.
3. Script Node adicional que percorre a árvore parseada do YAML e procura `name` duplicado dentro do mesmo array `fields` (para confirmar que nenhum campo aninhado colide com um objeto irmão já existente com o mesmo nome base) — 0 duplicados encontrados em toda a coleção.
4. `npm run build` — sucesso, `Compiled successfully`, TypeScript sem erros, todas as 30 páginas geradas (SSG/estático), sem warnings novos.
5. Inspeção do HTML gerado em `.next/server/app/quem-somos.html`: confirmado `"team-role","children":"CEO"` para Cipriano, `"Maquinista"` para Nome Exemplo 2, `"Vendedor"` para Nome Exemplo 3. Confirmado `grep -o 'Cargo Exemplo' .next/server/app/quem-somos.html` = 0 ocorrências (o valor morto/placeholder já não aparece em lado nenhum).
6. `JSON.parse` de `content/site/team.json` — OK, JSON válido.

Não corri o `/admin` num browser real (passo 5 do pedido, opcional/"se conseguires") — este ambiente de execução não tem browser interativo disponível para simular o handshake OAuth do Decap. As validações 1-6 acima cobrem a causa raiz (o YAML gerado agora usa o mesmo padrão `widget: object` já comprovadamente funcional nos outros ~50 campos do ficheiro que nunca tiveram este bug), mas a validação end-to-end no browser fica como ação recomendada ao Tester/QA, não à minha responsabilidade nesta ronda.

ISSUES (fora do âmbito, não corrigidos):
- O valor "Saler" em `content/site/team.json` (cargo em inglês do "Nome Exemplo 3") é provavelmente um erro ortográfico do utilizador (devia ser "Salesperson" ou "Seller") — mantido literal por instrução explícita do pedido; o utilizador que corrija via `/admin` se quiser.
- `npm run build` regenerou `next-env.d.ts` automaticamente (ficheiro auto-gerado pelo Next.js, mudou de `./.next/dev/types/...` para `./.next/types/...` porque `next dev` e `next build` escrevem caminhos diferentes). Não fiz parte do âmbito desta tarefa — é reescrito automaticamente por qualquer `next build`/`next dev` subsequente. Sinalizo para o Orchestrator decidir se inclui no commit ou não; não é uma alteração funcional.
- Não voltei a validar o `/admin` num browser real (ver VALIDATIONS acima) — fica como recomendação ao Tester, não é um blocker.

BLOCKERS: nenhum. Este trabalho não tocou nenhum sinal de escalonamento (autenticação/tokens/sessões, integração bancária, schema de produção, contrato de API consumido por cliente, dados pessoais de clientes) — é puramente configuração de CMS e correção de dados de conteúdo institucional de exemplo/placeholder.

REQUIRED_NEXT_ACTION: Tester deve:
1. Confirmar visualmente (build local ou preview) que `/quem-somos` e `/contactos` mostram os cargos corretos (Cipriano = "CEO", já confirmado por mim via HTML gerado, mas vale re-confirmar em navegador real).
2. Se possível neste ambiente, testar o fluxo real do `/admin`: editar um campo bilingue qualquer (ex.: "Cargo" de um membro da equipa) e confirmar no JSON resultante do editorial workflow que o objeto `{pt, en}` real foi atualizado, sem nenhuma chave nova com ponto a aparecer ao lado — este é o teste que prova definitivamente que o bug está corrigido na fonte, não só nos dados já recuperados manualmente.
3. Correr novamente o grep abrangente `grep -n 'name: "[a-zA-Z0-9]*\.[a-zA-Z0-9]*"' public/admin/config.yml` como parte da checklist de regressão, não só o grep do enunciado original (que tem falso-negativo em nomes com dígitos).
4. Rever os outros ~150 campos do `config.yml` que já usavam o padrão `widget: object` correto (não tocados por mim) para confirmar que nenhum deles tinha uma variante do mesmo bug escondida sob outro formato (ex.: nomes de dois níveis como o `buttons.whatsapp.pt` que encontrei e corrigi à parte do grep automático — fiz uma verificação manual de `\(PT\)|\(EN\)` residual nos labels depois da conversão automática e não sobrou nenhum, mas vale confirmação independente).

CONTEXT_FOR_NEXT_AGENT: A conversão dos 79 pares foi feita com um script Python que casa `- { label: "X (PT)", name: "campo.pt", widget: W, ...extras }` seguido imediatamente por `- { label: "X (EN)", name: "campo.en", widget: W2, ...extras }` no mesmo nível de indentação, e gera o bloco `widget: object` equivalente. Dois casos não foram apanhados automaticamente e tive de corrigir à mão, por ficarem fora do padrão simples:
(a) `buttons.whatsapp.pt`/`buttons.services.pt` em `hero.json` (linhas ~264 na versão nova) — nome de DOIS níveis (`campo.subcampo.pt`), não um nível; confirmei a estrutura real em `content/site/hero.json` (`buttons: {whatsapp: {pt,en}, services: {pt,en}}`) e gerei manualmente um `widget: object` de dois níveis para bater certo.
(b) O label do campo `goToSlideLabel` continha texto extra depois do "(PT)"/"(EN)" (`"Ir para o slide {n}" (PT) — tem de conter {n}`), que o script não limpou automaticamente (só removia o sufixo quando "(PT)"/"(EN)" estava mesmo no fim da string); removi manualmente o "(PT)" a meio da frase, mantendo o aviso "— tem de conter {n}" que é válido para ambos os idiomas.
Depois de aplicar estas duas correções manuais, corri um grep alargado (`\(PT\)|\(EN\)` em todo o ficheiro) para confirmar que não sobrava mais nenhum label mal limpo — resultado: 0 ocorrências. Recomendo ao Tester repetir essa mesma busca alargada como parte da checklist, porque cobre um sintoma que o próprio grep sugerido no enunciado não deteta.
