STATUS: COMPLETED
SUMMARY: Desenhados os 4 pontos pedidos, todos a partir de padrões visuais já existentes no site (sem novo sistema visual): (1) slider do hero da homepage com imagens + vídeo YouTube embed, autoplay lento só em imagens (pausa automática em vídeo, hover, foco e prefers-reduced-motion), setas+dots só com 2+ slides, sempre mudo/loop; texto do hero fixo por cima de todos os slides. (2) banners de campanha/contactos/servicos-lista/quem-somos replicam exatamente o padrão já validado `.page-hero.sd-hero` (imagem + overlay gradiente + texto sobreposto) do hero de serviço, sem variação nova, estáticos (não sliders). (3) cartões de serviço da homepage passam a usar o componente `ServiceCard` já existente, com override `homeTitle`/`homeBlurb` resolvido antes de entrar no componente (não dentro dele). (4) especificação do toggle "Secção visível" (booleano, default true, topo do formulário no Decap, ausência total do DOM quando desativado — sem placeholder em lado nenhum, sem display:none) aplicável a todos os blocos opcionais listados no FR-6.2 do product-analyst, incluindo regra de validação de build para garantir sempre pelo menos 1 slide visível no slider.
ARTIFACTS: project/state/task-001-migracao-stack/design-spec-fase3.md
ISSUES: Duas decisões técnicas ficaram delegadas ao developer/architect por não serem de UX: (a) forma de passar `homeTitle`/`homeBlurb` para o `ServiceCard` reutilizado — recomendo resolver o objeto Service antes de mapear, mantendo o componente sem conhecer o conceito "homepage override"; (b) legenda textual do vídeo do YouTube usada como `aria-label` do iframe — sugiro também mostrá-la como legenda discreta sobre o slide, mas é nice-to-have, não bloqueante.
BLOCKERS: nenhum.
REQUIRED_NEXT_ACTION: software-architect fecha: schema do slider da homepage (array de slides imagem|vídeo, min(1), campo `visible` por slide com validação de pelo menos 1 visível), schema dos banners novos (campanha/contactos/servicos-lista/quem-somos, mesmo padrão `bannerImage`+`bannerImageAlt` já usado no serviço), o campo `visible`/`enabled` transversal (nome, posição, default `true`) aplicado a todos os blocos do FR-6.2, e a nota de CSP para permitir `frame-src`/`img-src` do YouTube (`www.youtube-nocookie.com`) — restrição já fechada na Fase 4 que precisa de ser reaberta pontualmente para este caso.
CONTEXT_FOR_NEXT_AGENT: Ver design-spec-fase3.md para os detalhes completos, incluindo estados de erro/vazio (slider nunca fica vazio por decisão do utilizador; vídeo do YouTube que falhe a carregar não quebra a página, só fica sem imagem de fundo nesse slide) e acessibilidade (aria-label/aria-roledescription do carrossel, mute obrigatório no embed, pausa em hover/foco/reduced-motion). O padrão de banner do ponto 2 é deliberadamente idêntico ao do hero de serviço já implementado — não inventar variação nova. O toggle do ponto 4 não precisa de nenhuma tela nova (Decap gera UI a partir do schema), só do contrato de campo booleano especificado na secção 4 do design-spec.
