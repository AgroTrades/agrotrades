STATUS: COMPLETED
SUMMARY: Corrigido comentário enganoso em `next.config.mjs` junto à entrada de headers de `/images/uploads/:path*` (SEC-P5-10). O comentário anterior afirmava que a CSP desta rota era "cumulativa com a restrição de extensões em config.yml (raster apenas)" — essa restrição não existe em `public/admin/config.yml`. O novo comentário explica corretamente que: (1) esta CSP não é suficiente sozinha, (2) é contornável via `%2F` codificado no URL, que escapa a esta entrada e cai na CSP global (a qual permite scripts) — achado SEC-P5-09, e (3) a mitigação real é um GitHub Action (required check) ainda por desenhar/adicionar, que recusará ficheiros não-raster em `/public/images/uploads/`.
ARTIFACTS: next.config.mjs (linhas ~132-139, apenas comentário alterado; nenhuma linha de código/config funcional foi tocada)
VALIDATIONS: `npm run build` executado com sucesso — "Compiled successfully", TypeScript sem erros, geração estática das 30 rotas concluída sem falhas. Alteração é puramente textual (comentário), pelo que não há comportamento a testar via testes unitários; confirmado visualmente que as duas entradas de headers (`X-Content-Type-Options`, `Content-Security-Policy`) permanecem byte-a-byte inalteradas.
ISSUES: Nenhum problema novo encontrado fora do âmbito. O achado SEC-P5-09 (bypass via %2F) e SEC-P5-10 (comentário desatualizado) já estavam documentados por outros agentes; esta tarefa só corrigiu a documentação em código, não implementou a mitigação real (GitHub Action), que está fora de âmbito e a ser desenhada em paralelo por outro agente.
BLOCKERS: Nenhum.
REQUIRED_NEXT_ACTION: Nenhuma ação de Tester necessária — alteração é apenas de comentário/documentação em código, sem impacto funcional. Sugestão ao Orchestrator: quando o GitHub Action (required check) mencionado no comentário for criado, atualizar este mesmo comentário para referenciar o ficheiro de workflow real (atualmente descrito como "ainda por desenhar/adicionar").
CONTEXT_FOR_NEXT_AGENT: A CSP em `/images/uploads/:path*` (next.config.mjs ~L135-139) continua funcionalmente igual à da Fase 5 anterior — `sandbox; default-src 'none'` + `X-Content-Type-Options: nosniff`. O bypass via `%2F` (SEC-P5-09) e a ausência de restrição de extensões no Decap config.yml (SEC-P5-10) permanecem riscos reais não mitigados por código nesta tarefa; a mitigação planeada é um GitHub Action required check fora deste repositório de config Next.js. Ver `handoff-40-developer-fase5-fix.md` e `handoff-41-security-engineer-fase5-revalidacao.md` para o histórico completo destes achados.
